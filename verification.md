# 検証

## 自動検証

`how-to-update.md` の開発チェックを実行します。`test/status-test.cpp` は実際のファームウェアと同じJSONパーサー・状態計算を使用し、以下を確認します。

- API v1の正常応答、未来期限、分単位のカウントダウン
- 15分の同期停止境界、期限経過、推定期限の更新確認待ち
- nullと期限経過の区別、長時間経過のオーバーフロー防止
- 型違い・未対応スキーマ・不正な回数・未知の状態・不正な残り秒の拒否
- 不正応答で最後の正常状態を書き換えないこと

日本語字形生成では元ファイルのSHA-256と全文言の字形を確認します。

## 今回の結果（2026-09-05）

- 設定なし／非秘密の仮設定ありの両方で `pio run -e cyd` 成功。設定ありはRAM 51,144 bytes、Flash 914,541 bytes。仮設定は削除済み。
- MSVC C++17 `/W4 /WX` で状態テスト・描画テストをコンパイルし、両方成功。
- 実際の描画ソースをホスト上で実行し、96状態、最大桁の値、全文字の欠落と描画範囲を検証。320×240の生成画像も目視確認。
- 既存中継の `relay.test_relay` 10テスト成功（実HTTPソケット、認証分離、保持・拒否動作を含む）。実Androidとの接続ではありません。
- 日本語93字形の再生成成功。原データのSHA-256確認済み。
- フォント修正後は同じUnifontからASCII 95字形も生成し、日本語16×16・英数字8×16へ統一。旧8px文字描画の混入をテストで拒否し、96状態と最大桁の全文表示を再検証。修正後の320×240画像を目視確認し、`pio run -e cyd` も成功（Flash 576,505 bytes）。実機は使用していません。
- `pio check --skip-packages` は終了コード0ですが、toolchain defines抽出警告と依存ライブラリの解析警告があります。HIGHの `ConverterImpl.hpp` の `dummy` は実ソースで `T dummy = T();` と初期化されており、テンプレート解析の誤検出です。表示関数の未使用警告は実際の `setup/loop` 呼び出しとホスト描画テストで使用を確認しました。警告ゼロや包括的な脆弱性監査の合格とは扱いません。
- このPCのCMake自動検出はVisual Studioビルドツール構成を解決できなかったため、Developer Command Promptのコンパイラを直接使用しました。CI定義は追加済みですが、リモート実行は未実施です。

## 専用基板到着後の実機検証

接続済みのCodex Microを使用しません。新しい基板のポートを明示して書き込み、次を確認します。

1. 設定不足、Wi-Fi未接続、NTP遮断で原因が表示される。
2. 正しい証明書・読み取りトークンで取得でき、書き込みトークン・不正CA・別ホスト名で取得できない。
3. Androidの送信後、5分以内の次回取得で期限と適用回数が反映される。
4. 320×240画面で日本語・最大桁の回数・各状態が切れず、明るさ設定が反映される。
5. PC中継停止後も期限のカウントダウンが続き、通信失敗と同期停止が表示される。
6. 中継再開後に回復し、期限経過中・推定・要ログインを更新成功と表示しない。
7. 電源を再投入すると未受信になり、再取得後に表示が戻る。常時表示が通信断で消灯しない。

実機のTLS接続、液晶の描画品質、Wi-Fi復旧、長期連続動作は追加基板到着前のため未検証です。
