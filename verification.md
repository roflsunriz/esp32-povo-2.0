# 検証

## v0.7.0リリース（2026-09-23）

`main` とタグ `v0.7.0` をプッシュし、CI成功後にリリースワークフロー成功と公開アセットを確認した。公開ノートがCHANGELOGの0.7.0節と一致し、配布ソースZIPのSHA-256照合と主要ファイルの同梱を確認した。

## 自動消灯スライダーと取得残り時間表示（2026-09-23、実機確認済み）

作戦書 `esp32-ui-style.md` に基づき、自動消灯タブを固定選択から0〜59分・0〜24時間・取得間隔60〜600秒（60秒刻み）のスライダー＋スクロールバーへ変え、状態表示へ次の取得までの残り時間と取得中の表示を追加した。0分0時間は自動消灯オフ（常時点灯）と同じになり、従来の保存値は引き続き有効である。取得間隔はNVS `povo-display` の `poll_sec` へ保存し、旧記録は5分のまま移行する。新規文言の字形は `scripts/generate-font.py` の再生成で取り込み、CIの差分検査に対応する。

- ホスト6テスト・PlatformIOビルド・秘密値除去・監査・ビルドツール監査・設定画面テスト・フォント再生成が成功した。
- COM7基板（MAC 68:09:47:85:b2:2c）の全4,194,304バイトを `build/backup-povo-com7-20260923/flash.bin`（SHA-256 `50c357cb…5a17d2`）へ退避し、`verify-flash` のdigest一致を確認した。退避データにpovo識別子がありCodex/OpenCode識別子がないことを確認した。この個体はスタブ経路の読み出しが不安定なため、ROM経路（`--no-stub`）で退避・照合した。
- 書き換え直後の取得失敗は、Wi-Fiの立ち上がりが遅いことによる一過性である場合がある。再起動後は数分待ってから取得状態を判断する。
- 実機の表示・タッチ・ドラッグ・再起動後の保持は未検証である。書込時はアプリ領域（0x10000）のみ更新し、NVSの認証・設定値を維持する。
- COM7基板へアプリ領域のみ更新し、`verify-flash 0x10000` で一致を確認した。所有者が実機で上部タブ・紺背景・パネル・白枠バー・スライダー表示と操作、内容ドラッグ、0分0時間の常時点灯、毎秒の残り時間カウントダウン、取得回復を確認した。書き換え直後の取得失敗はWi-Fi立ち上がりの遅れによる一過性で、数分待つと回復した。タッチ到達範囲・行間隔・ドラッグ追従の修正も再確認した。

## 残り時間バーと期限間近の重点取得（2026-09-16）

残り時間の数値表示を維持し、状態画面の分表示の下へ減るタイプのプログレスバーを追加した。バー分母はトッピング有効期間（`Status::spanMs`、NVS `povo-display` の `span_ms` / `expiry_ms` へ保存）で、同じ期限の再取得では維持し、初回・変更時は取り直す。期限間近（残り30分以下）と期限切れ後は1分ごとに取得し、TLSハンドシェイク20秒・接続20秒・応答30秒・受信60秒へ延ばす（通常時は10秒・10秒・10秒・20秒のまま）。リニューで期限が延びると5分間隔へ自動復帰する。取得失敗時も重点期間中は1分後に再試行する。

ホスト6テスト（重点取得の境界・バー分母・1000分率、バー形状、バー描画の画素、重点時の受信制限60秒・通常時20秒の復帰を含む）、PlatformIOビルド、秘密値除去・監査・ビルドツール監査、設定画面テスト、フォント再生成の差分なしを確認した。字形の追加は不要だった。実機ではCOM6（CH340、位置1-1）のみ接続中で、COM3・COM4はゴースト表示のため対象外とした。COM6の自動リセットは従来の専用基板と同症状（`Wrong boot mode detected (0x13)`）で、手動で書き込みモードへ入れて対応する。

COM6の全4,194,304バイトをGit管理外の`build/backup-povo-com6-20260916/full-flash.bin`へ退避し、SHA-256を同ディレクトリへ記録した。既存パーティション表が新ビルドと一致し、既存アプリにpovoの識別子（`povo-setup-`・`povo-display`・`povo-auth`）がありCodexの識別子がないことを確認したため、COM6の0x10000アプリ領域だけを更新して`verify-flash 0x10000`で一致を確認した。全領域の再照合は不一致だったが、2回読み取りの差分6083バイトがNVS領域（0x9036〜0xD3F8）に限定されており、起動中のアプリ自身によるNVS書き込みと判断した。COM3は操作していない。バーの視認性、期限間近の1分更新、低速時の取得、リニュー後の復帰の実機目視は確認待ち。

## 専用基板の画面残像修正（2026-09-14）

v0.5.0を載せたCOM4の専用基板で、ユーザーがBOOT長押しによるペンの2点校正、ペンでのタブ切替、RST後もタブ操作が効くことを確認した。一方でタブを切り替えると中央から右側に前ページの大きな四角形が残り、表示が欠けた。TFT_eSPI 2.5.43の非virtualな`fillScreen`をTFT_eSPI基底ポインター経由で8-bit Spriteへ呼ぶと、基底クラスの初期幅240ピクセルだけが消去され、Spriteの320ピクセル全幅は消えないことが原因だった。

v0.5.1では320×240全体にvirtualな`fillRect`を呼ぶよう修正。右側に選択セルを描いた後に状態ページへ切り替え、右側が背景色に戻るホスト回帰テストを追加した。ホスト6テストとPlatformIOビルドが成功。既に照合済みの4MB全フラッシュ退避は保持し、COM4の0x10000アプリ領域だけを再更新して`verify-flash 0x10000`で一致を確認した。ユーザーがペンで両タブを数回切り替え、四角い残像と表示欠けが消えたと確認した。さらにRSTで再起動後もペンのタブ操作と欠けのない表示を確認した。COM3は操作していない。

## 追加専用基板へのv0.5.0更新（2026-09-14）

COM3のCodex Microと、ユーザーがpovo専用と説明したCOM4は異なるUSB接続位置のCH340と確認した。COM4の自動リセットは`Wrong boot mode detected (0x13)`で失敗。ユーザーの手動BOOT保持→RST短押し→BOOT解放でesptoolが接続し、フラッシュ容量4MBと確認した。COM3は操作していない。

COM4の全4,194,304バイトをGit管理外の`build/backup-povo-com4-20260914/full-flash.bin`へ退避し、`verify-flash 0x0`で実機と完全一致、SHA-256を同ディレクトリへ記録した。旧パーティション表が新ビルドと一致し、旧アプリがpovoであることを確認した。ユーザーが現行ローカルWi-Fi設定を実運用APと確認し、値を表示せずにビルドへ含まれることを確認した後、COM4の0x10000アプリ領域だけをv0.5.0へ書き込み、`verify-flash 0x10000`で一致を確認した。NVS、ブートローダー、パーティション領域は書き換えていない。

ユーザーがBOOT長押しからペンで2点校正でき、「自動消灯」タブをペンで切り替えられ、RST後もタブ操作が効くことを実機で確認した。タブ切替で画面残像が生じたため、上記v0.5.1で修正した。期限表示、消灯復帰、反転、長時間の差分描画とメモリは追加確認待ち。

v0.5.0のmain CIとタグ起点リリースは成功。公開ソースZIPには新しいタッチ・差分描画のソースが含まれ、Git管理外の`include/device-config.h`は含まれない。公開`SHA256SUMS.txt`とGitHub asset digestをZIPのSHA-256と照合した。

## ビルド依存の更新（2026-09-13）

PlatformIO 6.2.0、platformdirs 4.11.8、filelock 3.32.6、pip-api 0.0.35を隔離したPython環境へ `--no-deps` で導入した。旧PlatformIO番号への一致で失敗していた監査テストを修正し、監査テスト1件と秘密値除去テスト2件が成功。37個の明示依存の監査で既知脆弱性0件を確認した。

更新後のファームウェアビルド、ホスト6テスト、設定画面テスト、フォント再生成が成功した。Releaseでassertが無効になる既存問題も修正した。この依存更新では実機の書き込み・認証操作を実施していない。実機についての既存の未検証事項は下記に残す。

## 実機（2026-09-06）

現用CYD（COM3のCH340、ESP32-D0WD-V3）で確認した。書き込み前に全flash 4MBを退避し、検証後に書き戻す手順で実施した。STAはPCホットスポット（2.4 GHz帯・WPA2）へ接続した。5 GHz帯とWPA2/WPA3混在は未検証。

- 未認証状態で設定用Wi-FiのSSIDとパスワードを画面で読み取れた。スマホを接続し、メール送信・2分以内のOTP入力で認証に成功した。
- 保存後に設定用Wi-Fiが終了し、スマホのWi-Fi一覧から消えた。公式の適用中データ使い放題の期限（09/07 16:55 JST）とCYD表示が一致した。最終同期の更新も確認した。
- 電源抜き差し後、再ログインなしで同じ期限表示に戻り、設定用Wi-Fiは再表示されなかった。認証保持を確認した。
- ホットスポットOFF→ON後に自動再接続しない問題を見つけた。切断中は10秒ごとに再接続を試す修正を入れ、ケーブル抜き差しなしで残時間表示へ自動復帰することを確認した。
- 15分の同期停止の全時間・NVS破損・保存失敗・最大応答・複数トッピング・対象なし・長時間連続運用は実機未実施で、ホストテストの代替とする。

## 実測（2026-09-05）

- Pixel 10aのpovo 1.70.0-JP（857）でメールログインを採取。ログイン方法、OTP送信、認証確定がHTTP 200。キャプチャ用CA未登録とOkHttpピニングが別々の阻害要因であることも確認。
- アプリの認証を使わず、新しい端末IDの独立クライアントでメールOTP認証に成功。入力に時間がかかった最初の試行は401、再送後の入力は200。所有者がメールの有効期限2分を確認。
- アプリ停止・プロキシ無効の状態で、独立認証を保存・再読込し、`users/token` の認証更新と `user-plan-details-v2` の期限取得がともに200。
- 旧 `account/plan/details/get` は500 / ERROR_MYSQL。現行製品はこの経路を使わない。
- Pixelのプロキシ、今回のCA、計測ライブラリ、ADB転送、中間ファイルを撤去済み。APKの差し替え・再インストールはしていない。

保存した実測構造、静的根拠、再採取方法は [docs/auth-capture](docs/auth-capture/README.md) にあります。**これらはESP32ファームウェアを実機実行した結果ではありません。**

## 自動検証

`how-to-update.md` の開発チェックを実行します。

- `status-test`: カウントダウン、15分の同期停止境界、期限経過、時刻の飽和、不明期限、重点取得の境界（残り30分以下・期限切れ）、リニュー判定、バー分母の維持・再取得、1000分率の縮小。
- `auth-protocol-test`: 要求JSON、未知の追加認証、dashboard成功、PIN、JWT構造とissuer、期限フィールド、不正応答で既存認証を上書きしないこと。
- `client-test`: 実クライアントにHTTP・時計・保存のフェイクを接続。OTP120秒、再送30秒、保存失敗、認証更新の一時失敗、401の単発再試行、チャンク転送・途中切断・サイズ・時間制限、重点時の受信制限60秒と通常時20秒への復帰。
- `direct-status-test`: 日本語の午前/午後、日付検証、対象トッピングの抽出、不正応答で最後の期限を上書きしないこと。
- `ui-test`: 実描画ソースの文字欠落・範囲、期限状態、残り時間バーの塗り・空き・期限切れの画素、設定用Wi-Fi表示、タブと消灯設定32件・画面反転の描画。ホスト画像も確認する。
- `display-settings-test`: 消灯32件の表と刻み、表示文言、残り時間バーの形状と塗り幅、タブ・設定グリッドの当たり判定、消灯境界、画面反転、BOOTのチャタリング除去と1回押し判定、タッチ近接3点と8-bit描画の差分帯・連続帯の判定。
- `portal-test.mjs`: 実設定HTMLをChromeで起動。メール入力、OTP画面、残時間、失敗した再送で期限を延ばさないこと、成功、通信断からの復帰、320×568・360×780・1920×1080。
- `capture-auth-test.py`: 秘密値と動的キーの除去、配列の記録上限。

CMake/MSVC `/W4 /WX` の5テストとChrome検証に成功。Pixel検証中の独立認証から取得した実応答も同じC++期限パーサーで読み、未来の適用期限を得た。生応答は秘密を含み得るためGit管理外で破棄する。PlatformIOの直接認証版ビルドも成功。さらに新規Python仮想環境へ37個の明示パッケージだけを導入し、PlatformIO Home用サーバー依存なしでビルド成功、例外なしの既知脆弱性監査で検出0件を確認した。サンドボックス内でビルドツールが起動できない場合は同じコマンドを通常権限環境で再実行して確認している。

## 実機確認の残り

使用中のCodex Micro基板を使う場合は事前に全flashを退避し、検証後に書き戻して `CODEX_CYD_READY` まで確認する。2026-09-06はこの手順で現用基板を確認した。追加の専用基板が着いた場合も次を確認する。

1. Wi-Fi/NTP/証明書不正時に原因を表示し、未認証状態では設定用Wi-FiのSSIDとパスワードを読み取れる。
2. スマホを接続し、メール送信・2分以内のOTP・期限切れ・再送・追加認証要求を確認する。
3. 保存後に設定用Wi-Fiが終了し、公式の適用中データ使い放題と期限が一致する。
4. 再起動後の認証保持、トークン更新、認証拒否後の再ログインを確認する。
5. 通信断でも最後の正常期限からカウントダウンし、15分で同期停止。復旧時に正常取得へ戻る。
6. NVS破損・保存失敗・最大応答・複数トッピング・対象なしを確認する。
7. 指と普段使うペンでタブ切替、スライダーの分・時間・取得間隔の選択・保存・復帰、スクロールとドラッグ、消灯中の取得継続、タッチ復帰を確認する。BOOT短押しで反転し、長押しで左上・右下の十字を押して位置と押圧感度を調整する。調整後の両向きのタッチ、再起動後の保持、タッチしない時の誤操作なし、反応しないペンでの15秒タイムアウトと旧値保持を確認する。使用量の定期更新、タブ切替、回転、消灯復帰で画面に欠け・ちらつきがなく、HTTPS取得中の8-bit Sprite用メモリも足りることを確認する。
8. 長時間連続運用でヒープ使用量、Wi-Fi復旧、液晶の視認性、設定用APとの共存を確認する。
9. 残り時間バーが数値表示と矛盾なく縮むこと、残り30分以下と期限切れ後に1分ごとへ切り替わること、低速回線でも取得できること、リニュー後に5分ごとへ戻ることを確認する。

ESP32上の実TLS接続、SoftAP/STA同時動作、NVSの実電源断、液晶実表示、短時間のWi-Fi復旧は2026-09-06に実機確認した。2026-09-14には専用基板でペン校正、タブ操作と画面残像の修正を確認した。15分の同期停止の全時間、トークン更新の長期確認、認証拒否後の再ログイン、長期運用、自動消灯・タッチ復帰・BOOT反転は未検証であり、ホストテストの代替とする。
