# 検証

## 実測（2026-09-05）

- Pixel 10aのpovo 1.70.0-JP（857）でメールログインを採取。ログイン方法、OTP送信、認証確定がHTTP 200。キャプチャ用CA未登録とOkHttpピニングが別々の阻害要因であることも確認。
- アプリの認証を使わず、新しい端末IDの独立クライアントでメールOTP認証に成功。入力に時間がかかった最初の試行は401、再送後の入力は200。所有者がメールの有効期限2分を確認。
- アプリ停止・プロキシ無効の状態で、独立認証を保存・再読込し、`users/token` の認証更新と `user-plan-details-v2` の期限取得がともに200。
- 旧 `account/plan/details/get` は500 / ERROR_MYSQL。現行製品はこの経路を使わない。
- Pixelのプロキシ、今回のCA、計測ライブラリ、ADB転送、中間ファイルを撤去済み。APKの差し替え・再インストールはしていない。

保存した実測構造、静的根拠、再採取方法は [docs/auth-capture](docs/auth-capture/README.md) にあります。**これらはESP32ファームウェアを実機実行した結果ではありません。**

## 自動検証

`how-to-update.md` の開発チェックを実行します。

- `status-test`: カウントダウン、15分の同期停止境界、期限経過、時刻の飽和、不明期限。
- `auth-protocol-test`: 要求JSON、未知の追加認証、dashboard成功、PIN、JWT構造とissuer、期限フィールド、不正応答で既存認証を上書きしないこと。
- `client-test`: 実クライアントにHTTP・時計・保存のフェイクを接続。OTP120秒、再送30秒、保存失敗、認証更新の一時失敗、401の単発再試行、チャンク転送・途中切断・サイズ・時間制限。
- `direct-status-test`: 日本語の午前/午後、日付検証、対象トッピングの抽出、不正応答で最後の期限を上書きしないこと。
- `ui-test`: 実描画ソースの文字欠落・範囲、期限状態、設定用Wi-Fi表示。ホスト画像も確認する。
- `portal-test.mjs`: 実設定HTMLをChromeで起動。メール入力、OTP画面、残時間、失敗した再送で期限を延ばさないこと、成功、通信断からの復帰、320×568・360×780・1920×1080。
- `capture-auth-test.py`: 秘密値と動的キーの除去、配列の記録上限。

CMake/MSVC `/W4 /WX` の5テストとChrome検証に成功。Pixel検証中の独立認証から取得した実応答も同じC++期限パーサーで読み、未来の適用期限を得た。生応答は秘密を含み得るためGit管理外で破棄する。PlatformIOの直接認証版ビルドも成功。サンドボックス内でビルドツールが起動できない場合は同じコマンドを通常権限環境で再実行して確認している。

## 専用CYD到着後の実機確認

使用中のCodex Micro基板は検証対象外です。専用基板だけで次を確認します。

1. Wi-Fi/NTP/証明書不正時に原因を表示し、未認証状態では設定用Wi-FiのSSIDとパスワードを読み取れる。
2. スマホを接続し、メール送信・2分以内のOTP・期限切れ・再送・追加認証要求を確認する。
3. 保存後に設定用Wi-Fiが終了し、公式の適用中データ使い放題と期限が一致する。
4. 再起動後の認証保持、トークン更新、認証拒否後の再ログインを確認する。
5. 通信断でも最後の正常期限からカウントダウンし、15分で同期停止。復旧時に正常取得へ戻る。
6. NVS破損・保存失敗・最大応答・複数トッピング・対象なしを確認する。
7. 長時間連続運用でヒープ使用量、Wi-Fi復旧、液晶の視認性、設定用APとの共存を確認する。

ESP32上の実TLS接続、SoftAP/STA同時動作、NVSの実電源断、液晶実表示、長期運用は未検証です。HTTP/保存フェイクとビルドはその代替であり、実機成功とは扱いません。
