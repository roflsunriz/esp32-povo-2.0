USER:
CYD ESP32 ILI9341 2432S028Rでpovo 2.0のexpire dateとremaining hours and daysを常時表示しておくのは良いアイディア？プロジェクトは"esp32-codex-notifications"と"povo-2.0-revanced"に関連がある。ReVancedパッチ版povo 2.0アプリ自体にリニューは任せておくとして、APIを使って数分おきに更新する、みたいな。

ASSISTANT:
**良いアイディアです。自動更新をアプリに任せ、その結果と残り時間をCYDで常時確認する構成は、相性が良いと思います。** 両プロジェクトのREADMEも確認しました。

特に価値があるのは、残り時間だけでなく、**「更新後の新しい期限を確認できたか」「再ログインなどで止まっていないか」**が一目でわかることです。

ただし、APIの使い方は少し工夫したいです。

- `povo-2.0-revanced`は、ログイン済みアプリのAPIクライアントを再利用し、現在の終了日時を取得する仕組みが既にあります。
- `esp32-codex-notifications`は現在BLE専用で、Wi-Fiは未使用です。表示用通信を追加する必要があります。
- ESP32からpovo APIを直接呼ぶ方式では、認証・セッション更新も別途実装することになります。**まずはアプリが取得した期限を、小さな表示用API経由でCYDへ渡す方式を推します。**

構成案はこうです。

```text
povo API
   ↓ 既存の認証・期限取得
パッチ版povoアプリ ── 自動更新
   ↓ 期限・更新状態を送信
表示用の中継API
   ↑ 5分おきに取得
CYD ── 残り時間は端末内で毎分計算
```

**API取得は5分おき、カウントダウン表示は毎分更新**で十分です。期限がわかれば、残り時間の計算に通信は不要です。アプリ側は更新成功・失敗・期限変更の際にも送信すると、表示への反映が早くなります。中継APIのポーリングごとにpovoへ問い合わせる必要はありません。

画面は例えば、この程度に絞ると見やすそうです。

```text
povo  データ使い放題

       残り 3日 14時間
          86時間12分

期限  09/08 23:59 JST
自動更新 ON  ·  コード適用 4/24
最終同期 2分前
```

320×240のCYDなら、この情報量に収める設計が現実的です。[対応基板の表示仕様](https://github.com/RuiSantosdotme/ESP32-TFT-Touchscreen)

実装時に押さえたい点は3つです。

1. **期限を区別する。** メインは「現在のトッピング終了日時」。プリペイドコードの入力期限は別項目にします。
2. **通信断を見えるようにする。** 保存済み期限で計算を続けつつ「同期停止」を表示。期限を過ぎても新しい期限を受け取れていなければ、「更新確認待ち」とします。
3. **常時表示の消灯ルールを分ける。** 現行ファームウェアはBLE切断後30秒で消灯するため、povo表示には独立した明るさ・消灯設定が必要です。

同じCYDで兼用するなら、**povoを通常画面にし、Codex通知が来たときだけ一時表示、タッチで既存画面へ切り替え**が使いやすそうです。残り時間と自動更新の成否を常に確認できる、実用的な拡張だと思います。