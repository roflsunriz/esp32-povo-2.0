# 開発への参加

`AGENTS.md` と作業環境の `COMMON-AGENTS.md` を確認してください。既存API v1との互換性を保ち、状態計算・通信・描画を分離します。変更には必要な検証と日本語のCHANGELOG更新を含めます。

接続済みCodex Microでの検証は禁止です。専用基板の到着前はホストテストとコンパイルを使用し、実機確認済みとは記載しません。更新方法は `how-to-update.md` を参照してください。
